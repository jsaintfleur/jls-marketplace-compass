# Missing Work Package

Implementation is blocked because `WORKPACKAGE.md` is not present in this repo.

The handoff file requires the source package `project-4-marketplace-compass.md` to be copied here as `WORKPACKAGE.md` before tickets MC-01 through MC-14 can be executed.
